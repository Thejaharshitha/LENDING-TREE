@model Account.Models.LoginViewModel
@{
    ViewBag.Title = "Login";

}

<h2>Login</h2>

<div>
    @using (Html.BeginForm("Login", "Account"))
    {
        <div style="color:red;">@Html.ValidationSummary()</div>
        <div class="row">
            <!--Show details are saved successfully message-->
            <div class="col-lg-12">@ViewBag.Message</div>
        </div><br />

        <div class="row">
            <div class="col-lg-2">@Html.LabelFor(a => a.UserId)</div>
            <div class="col-lg-10">@Html.TextBoxFor(a => a.UserId, new { @class = "form-control" })</div>
            <div class="col-lg-10">@Html.ValidationMessageFor(a=>a.UserId,"",new { @class = "text-danger" })</div>
        </div><br />
              <div class="row">
                  <div class="col-lg-2">@Html.LabelFor(a => a.Password)</div>
                  <div class="col-lg-10">@Html.TextBoxFor(a => a.Password, new { @class = "form-control", type = "Password" })</div>
                  <div class="col-lg-10">@Html.ValidationMessageFor(a => a.Password, "", new { @class = "text-danger" })</div>
              </div><br />
        <input type="submit" value="Login" class="btn btn-primary" />
    }
</div>